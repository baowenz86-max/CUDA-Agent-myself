[project]
name = "cuda-agent"
version = "0.1.0"
description = "Agentic workflow for generating and optimizing CUDA kernels"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "datasets>=2.0",
]

[project.scripts]
cuda-agent = "cuda_agent.workflow:main"
cuda-agent-download = "scripts.download_dataset:main"
